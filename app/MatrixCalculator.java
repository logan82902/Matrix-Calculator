package app;

public class MatrixCalculator{
    /** 
     * Adds two given matrices together.
     * Matrices are expected to be in format 
     * "1 2 3 
     *  4 5 6
     *  7 8 9"
     * with elements separated by spaces and rows separated by newlines.
     * @param matrix1 The first matrix to add.
     * @param matrix2 The second matrix to add.
     * @return The result matrix, in same format as the input.
     * 
    */
    public String add(String matrix1, String matrix2) {
        return "0";
    }

    /** 
     * Subtracts the second matrix from the first.
     * Matrices are expected to be in format 
     * "1 2 3 
     *  4 5 6
     *  7 8 9"
     * with elements separated by spaces and rows separated by newlines.
     * @param matrix1 The matrix to subtract from.
     * @param matrix2 The matrix being subtracted.
     * @return The result matrix, in same format as the input.
     * 
    */
    public String subtract(String matrix1, String matrix2) {
        return "0";
    }

    /** 
     * Multiplies two matrices together.
     * Matrices are expected to be in format 
     * "1 2 3 
     *  4 5 6
     *  7 8 9"
     * with elements separated by spaces and rows separated by newlines.
     * @param matrix1 The first matrix to multiply.
     * @param matrix2 The second matrix to multiply.
     * @return The result matrix, in same format as the input.
     * 
    */
    public String multiply(String matrix1, String matrix2) {
        return "0";
    }

    /** 
     * Calculates the dot product of two matrices.
     * Matrices are expected to be in format 
     * "1 2 3 
     *  4 5 6
     *  7 8 9"
     * with elements separated by spaces and rows separated by newlines.
     * @param matrix1 The first matrix to operate on.
     * @param matrix2 The second matrix to operate on.
     * @return The result matrix, in same format as the input.
     * 
    */
    public String dotProduct(String matrix1, String matrix2) {
        return "0";
    }

    /** 
     * Calculates the inverse of a matrix.
     * Matrices are expected to be in format 
     * "1 2 3 
     *  4 5 6
     *  7 8 9"
     * with elements separated by spaces and rows separated by newlines.
     * @param matrix The matrix to calculate the inverse for.
     * @return The result matrix, in same format as the input.
     * 
    */
    public String inverse(String matrix) {
        return "0";
    }

}
